// generated by `npm run generate-configs`
/* eslint-disable */
const path = require('path');
const { rootDir } = require('../lib/common');
const paths = {
  totara_core: './totara/core/tui/js',
  'totara_core/containers': './totara/core/tui/containers',
  'totara_core/presentation': './totara/core/tui/presentation',
  'totara_core/pages': './totara/core/tui/pages',
};
module.exports = Object.entries(paths).reduce((acc, [key, val]) => {
  acc[key] = path.join(rootDir, val);
  return acc;
}, {});
