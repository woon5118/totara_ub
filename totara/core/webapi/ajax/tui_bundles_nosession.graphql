query totara_core_tui_bundles_nosession(
  $components: [String!]!
  $theme: String!
) {
  bundles: totara_core_tui_bundles(components: $components, theme: $theme) {
    id
    component
    name
    type
    url
  }
}
