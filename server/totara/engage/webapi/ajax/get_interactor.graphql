query totara_engage_get_interactor($resource_id: core_id!) {
  interactor: totara_engage_interactor(resource_id: $resource_id) {
    user_id
    can_bookmark
    can_comment
    can_react
    can_share
  }
}
