mutation mod_perform_toggle_notification_recipient(
  $input: mod_perform_toggle_notification_recipient_input!
) {
  mod_perform_toggle_notification_recipient(input: $input) {
    notification {
      id
      name
      class_key
      active
      recipients {
        relationship_id
        name
        active
      }
      trigger_label
      triggers
    }
  }
}
