mutation mod_perform_activate_activity(
  $input: mod_perform_activate_activity_input!
) {
  mod_perform_activate_activity(input: $input) {
    activity {
      id
      name
      state_details {
        code
        display_name
      }
    }
  }
}
