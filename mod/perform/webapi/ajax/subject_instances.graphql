query mod_perform_subject_instances(
  $participant_id: core_id
  $filters: mod_perform_subject_instance_filters
) {
  mod_perform_subject_instances(
    participant_id: $participant_id
    filters: $filters
  ) {
    id
    progress_status
    activity {
      name
    }
    subject_user {
      fullname
    }
    participant_instances {
      progress_status
      relationship_name
    }
  }
}
