mutation mod_perform_update_track_schedule_closed_fixed(
  $track_schedule: mod_perform_track_schedule_closed_fixed_input!
) {
  mod_perform_update_track_schedule_closed_fixed(
    track_schedule: $track_schedule
  ) {
    track {
      id
      description
      status
      schedule_type
      assignments {
        type
        group {
          id
          type
          type_label
          name
        }
      }
    }
  }
}
