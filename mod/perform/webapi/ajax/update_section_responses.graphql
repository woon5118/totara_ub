mutation mod_perform_update_section_responses(
  $input: mod_perform_update_section_responses_input!
) {
  mod_perform_update_section_responses(input: $input) {
    id
    section {
      id
      title
    }
    element_responses {
      section_element {
        id
        sort_order
      }
      response_data
      validation_errors {
        error_code
        error_message
      }
    }
  }
}
