query mod_perform_notification_info($activity_id: core_id!) {
  mod_perform_notification_info(activity_id: $activity_id) {
    id
    name
    active
    trigger_count
    recipients {
      name
      active
    }
    triggers
  }
}
