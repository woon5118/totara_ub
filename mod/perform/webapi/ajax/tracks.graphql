query mod_perform_tracks($activity_id: param_integer!) {
  mod_perform_tracks(activity_id: $activity_id) {
    id
    description
    status
    assignments {
      track_id
      type
      group {
        id
        type
        type_label
        name
      }
    }
  }
}
