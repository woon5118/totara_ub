query mod_perform_default_track($activity_id: param_integer!) {
  mod_perform_default_track(activity_id: $activity_id) {
    id
    description
    status
    assignments {
      type
      group {
        id
        type
        type_label
        name
      }
    }
  }
}
