query mod_perform_subject_instance($subject_instance_id: core_id!) {
  mod_perform_subject_instance(subject_instance_id: $subject_instance_id) {
    id
    progress_status
    activity {
      name
    }
    subject_user {
      id
      fullname
      profileimageurlsmall
    }
    participant_instances {
      id
      progress_status
      relationship_name
    }
  }
}
