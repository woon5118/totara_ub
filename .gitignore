# User added files
/server/config.php
/.graphqlconfig

# Tooling files
phpunit.xml
phpcs.xml
/behat.yml
/behat_local.yml
# Composer support - only composer.json is to be in git, the rest is installed in each checkout.
/composer.phar
/composer.lock
/vendor/
# Node modules for the TUI componentry
/node_modules/
# Node modules for Totara
/server/node_modules/
# Codechecker tool
/server/local/codechecker/

# Build files we don't want.
# AMD build files
/server/**/amd/build/
# YUI build and coverage coverage
/server/**/yui/build/
/server/lib/yuilib/*/build/
/server/lib/yuilib/*/*/*-coverage.js
# Coverage report generated by jest --coverage
/coverage/
# The component map required for development
/component_map.json
# The jsconfig.json file used by VSCode Vue extension: npm run generate-config
/jsconfig.json
# Webpack aliases file used to help PHPStorm: npm run generate-config
/client/scripts/generated/webpack_aliases.js
# Client build directories
/client/build/*/

# Safety measure. Private key should never be added to repository
totara_private.pem

# File resulting from conflicts
*.swp
*.*.orig
*.*.rej

# ctag and cscope data files
/tags
/TAGS
/cscope.*

# IDE specific stuff
# Jetbrains IDEs
/.idea/
atlassian-ide-plugin.xml
# Netbeans
/nbproject/
# Eclipse
/.settings/
/.project
/.buildpath

# OS specific stuff
.DS_Store
Thumbs.db

# Old dreamweaver support
_notes
diff.log

# Unknown - are these required?
*~
/.patches/
CVS
/.cache
