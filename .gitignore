# User added files
/config.php
/.graphqlconfig

# Tooling files
phpcs.xml
/behat_local.yml
# Composer support - only composer.json is to be in git, the rest is installed in each checkout.
composer.phar
composer.lock
/vendor/
# Node modules for the TUI componentry
/node_modules/
# PHPUnit profiling artefacts in the srcroot directory
/phpunit_profile_methods_*.csv
/phpunit_profile_classes_*.csv
# PHPUnit result cache artefact generated when it is running
.phpunit.result.cache

# Coverage report generated by jest --coverage
/coverage/
# The component map required for development
/component_map.json
# The jsconfig.json file used by VSCode Vue extension: npm run generate-config
/jsconfig.json

# Safety measure. Private key should never be added to repository
totara_private.pem

# File resulting from conflicts
*.swp
*.*.orig
*.*.rej

# ctag and cscope data files
/tags
/TAGS
/cscope.*

# IDE specific stuff
# Jetbrains IDEs
/.idea/
atlassian-ide-plugin.xml
# Netbeans
/nbproject/
# Eclipse
/.settings/
/.project
/.buildpath

# OS specific stuff
.DS_Store
Thumbs.db

# Old dreamweaver support
_notes
diff.log

# Unknown - are these required?
*~
/.patches/
CVS
/.cache
